export default function NotFound() {
  return (
    <span
      className="font__feedback no-data"
      title="Font name is bad/unavailable or doesn't include any needed character."
    >
      @font-face/characters not found.
    </span>
  );
}
