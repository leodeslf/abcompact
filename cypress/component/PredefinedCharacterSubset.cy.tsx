/// <reference types="cypress" />

import React from 'react';
import PredefinedCharSubset from
  '../../src/components/form/PredefinedCharSubset';

describe('PredefinedCharSubset', () => {
  it('renders and has the correct data', () => {
    // cy.mount(<PredefinedCharSubset id={1} label='Arabic' />);
    // cy.get('.predefined-char-subsets__subset');
  });
});
